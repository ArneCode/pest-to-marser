expr = { term ~ ( #op = ("+" | "-") ~ term )* }
term = { factor ~ ( #op = ("*" | "/") ~ factor )* }
factor = { #inner = number | "(" ~ #inner = expr ~ ")" }
number = @{ ASCII_DIGIT+ }
WHITESPACE = _{ " " | "\t" }
